# Authoritative dependency management is pyproject.toml + uv.lock (use `uv sync`).
# This file is kept for MCP Inspector / plain-pip users:  pip install -r requirements.txt
mcp[cli]
python-dotenv
httpx
youtube-transcript-api
# Screenshot / visual-capture feature (tools/screenshots.py).
# imageio-ffmpeg ships a static ffmpeg binary, so the YouTube frame path works
# without a system-wide ffmpeg install (a system ffmpeg on PATH is still preferred).
pymupdf
pillow
yt-dlp
imageio-ffmpeg
